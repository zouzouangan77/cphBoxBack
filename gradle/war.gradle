apply plugin: "war"

bootWar {
    mainClass = "com.psu.rouen.cphbox.CphBoxBackApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
